# Maps

Output of Phase Q5 (QGIS Print Layout + Atlas): one situation map per
upazila, generated from `data/processed/upazila_priority_ranking.gpkg`
as the atlas coverage layer.

- `sunamganj_flood_priority_atlas.pdf` — the full atlas, one page per
  upazila, each showing: priority score, its three input criteria,
  flood extent, degraded road network, health facilities, legend,
  scale bar, north arrow.
